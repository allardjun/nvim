# Jun's neovim config dotfiles

## To install

1. Clone me

```
cd ~/.config/
git clone <<this repo>>
```

This requires a more-recent version of neovim than the one provided in some aws ubuntu instances. 
